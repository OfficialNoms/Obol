</main>
    <footer class="text-center text-sm text-gray-500 mt-12 pt-8 border-t border-white/10">
        <div class="flex justify-center gap-x-4 mb-4">
            <a href="/terms" class="hover:text-white">Terms</a>
            <span>&bull;</span>
            <a href="/privacy" class="hover:text-white">Privacy</a>
            <span>&bull;</span>
            <a href="/help" class="hover:text-white">Help</a>
        </div>
      <p>Obol Token Manager v<%= pkg.version %></p>
      <p>Copyright 2025 © <a href="https://nomm.me" class="hover:text-white">Nomm.me</a>, All Rights Reserved.</p>
      <% if (process.env.KOFI_LINK) { %>
        <p class="mt-2">Support development by <a href="<%= process.env.KOFI_LINK %>" target="_blank" class="text-yellow-400 hover:underline">buying me a coffee</a>! ☕</p>
      <% } %>
    </footer>
  </div>

  <div
    x-data="{ showBanner: false }"
    x-init="showBanner = !localStorage.getItem('cookie_consent_accepted')"
    x-show="showBanner"
    x-transition:enter="transition ease-out duration-300"
    x-transition:enter-start="opacity-0 transform translate-y-4"
    x-transition:enter-end="opacity-100 transform translate-y-0"
    x-transition:leave="transition ease-in duration-300"
    x-transition:leave-start="opacity-100 transform translate-y-0"
    x-transition:leave-end="opacity-0 transform translate-y-4"
    class="fixed bottom-0 inset-x-0 pb-2 sm:pb-5 z-10"
    style="display: none;"
    >
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="p-3 rounded-lg bg-bg-light border border-gray-700 shadow-lg flex items-center justify-between flex-wrap">
            <div class="w-0 flex-1 flex items-center">
                <p class="ml-3 font-medium text-white">
                    <span>We use essential cookies for site functionality, such as keeping you logged in. By using this site, you acknowledge their use.</span>
                </p>
            </div>
            <div class="order-3 mt-2 flex-shrink-0 w-full sm:order-2 sm:mt-0 sm:w-auto">
                <button
                    @click="showBanner = false; localStorage.setItem('cookie_consent_accepted', 'true')"
                    class="flex items-center justify-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-brand-600 hover:bg-brand-500"
                >
                    Acknowledge
                </button>
            </div>
        </div>
    </div>
  </div>

</body>
</html>