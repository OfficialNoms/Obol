<!DOCTYPE html>
<html lang="en" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Obol Dashboard</title>
    <link rel="stylesheet" href="/css/output.css" />
    <script defer src="/vendor/alpine.min.js"></script>
  </head>
  <body class="bg-bg-dark text-gray-200 antialiased">
    <div class="max-w-7xl mx-auto p-4 sm:p-6 lg:p-8">
      <header class="flex justify-between items-center mb-8">
        <a href="/" class="flex items-center space-x-3">
          <img src="/images/logo.png" alt="Obol Logo" class="h-10 w-10 rounded-lg" />
          <h1 class="text-2xl font-bold">Obol<span class="text-brand-500">.</span></h1>
        </a>
        <nav class="flex items-center">
          <% if (user) { %>
          <a href="/dashboard" class="text-sm font-semibold text-gray-400 hover:text-white mr-4">Dashboard</a>
          <a href="/help" class="text-sm font-semibold text-gray-400 hover:text-white mr-4">Help</a>
          <span class="text-sm text-gray-400 mr-4">Logged in as <%= user.username %></span>
          <a href="/logout" class="text-sm font-semibold text-gray-400 hover:text-white">Log out &rarr;</a>
          <% } else { %>
          <a
            href="/login"
            class="rounded-md bg-brand-600 px-3.5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-brand-500"
            >Login with Discord</a
          >
          <% } %>
        </nav>
      </header>
      <main>